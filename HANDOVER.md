# HANDOVER.md — זיכרון בין-סשן

קובץ זה מתוחזק על-ידי הסקיל `/handover`. מתעדכן **רק** כשסשן מייצר משהו שהסשן הבא צריך לדעת — אילוץ, footgun, החלטה מגבילה. לא פעולות שגרתיות.

---

## סשן 2026-08-12 — סיווג-קידוחים, מעטפות-אתר, סבב-משוב-קישון, חקירת-PFBA

- **הקישון בלבד עודכן; חגית/קיסריה מיושנים בכוונה** (הוראת-משתמש): סבב-המשוב (#17 — מסנן-אגן, שמות-תחנות, צמתים-בשמות, כפתורי-קבוצה, החדרת-גדות, פרוזה פשוטה) מומש בקוד המשותף אך רק דוח-הקישון חודש. **שינויי-המנוע כבר משנים את חגית/קיסריה בקוד** (כלל-הלכידה יהפוך את "מק טירלי 5" ו"מק. קיסריה 4" למועמדי-שרשרת) — עם אישור תבנית-הקישון יש להריץ סבב-עדכון מלא: regenerate + סנכרון claims/נרטיב + republish לשני התיקים.
- **מסנן-אגן מוצהר** (`basin_screen` ב-region.json, מיושם ב-`_prepare`): תחנות שמסלול-ה-D8 שלהן לא מתלכד עם גזע-התיק מושמטות לפני הייחוס + נספח א'. מוצהר רק בקישון (12 תחנות דליה); תיק חדש צריך להצהיר בעצמו.
- **⚠ לקח (מופע ראשון — טרם חוק)**: שינוי-מנוע משנה בשקט מספרים שטענות נשענות עליהם. מעטפות-האתר (27e0fd2) רוקנו את "הסדרה העצמאית" של אלרואי (3 נקודות תוך-אתר) והשטיחו את דעיכת-RD ‏(r=-0.65←-0.02) — נתפס רק בסבב הזה; KI-D1/KI-C2 עודכנו. אחרי כל שינוי-מנוע: להריץ ולהשוות מול claims לפני commit.
- **חקירת-עומק PFBA** (זוג "100%" סקר-7↔יקנעם 2, PROCESS ‏#18): הדמיון הוא ארטיפקט חד-תרכובתי; סקר-7 = שחרור-C4 נקודתי מתמשך (מועמד: ממסי HFE-7100/7200 או HFBA ישיר; PFBS/PFBA=0.001, צוק ×180), יקנעם 2 = חזית-פלומה/ארטיפקט-תאריכים. מסקנות+מבחינים ב-KI-D4/KI-D2, פעולות KI-ACT-8/9. **אילוץ אנליטי קבוע: הפאנל מתחיל ב-C4 — TFA/PFPrA אינם נמדדים.**
- **סוכנים חסכוניים — הוראת-משתמש קבועה**: ביצוע מובנה ב-haiku/sonnet (‏QA-דפדפן, אפיון, מחקר-רשת); שיפוט אצל המודל הראשי. אימות עצמאי חובה — ‏haiku פספס באג-bidi ("Water Light 3M") ודיווח FAIL-שווא על כפתורי-הקבוצה; צילומי-המסך נבדקים תמיד גם עצמאית.
- **ממתין להכרעת המשתמש (הרשימה העדכנית; מחליפה את של 08-11)**: אישור תבנית/סבב-קישון (#12+#17) ← מפעיל את סבב-העדכון המלא · חגית: D2 (r=-0.7), C1 · קיסריה: KD1 · קישון: KI-A2/A3, KI-C1/C2, KI-D5 (יגור-שרשרת) · פסקאות-נרטיב (טיוטות-מודל) בשלושת התיקים · נדחו: ספיקות, חצי-כמותי.

---

## סשן 2026-08-11 — תבנית משפחות-הסעה, מבחן-הקישון העיוור, שלושה תיקים

- **תבנית דוח-התיק שוכתבה סביב 6 משפחות-הסעה** (מוקד/נחל/שאיבה/מתועל/החדרת-גדות/תהום) + CSM + סרגל-סינון + בחירת-תחנות במטריצה — דרישה #12 ב-PROCESS פתוחה, עם **יומן סבב-משוב** (5 הערות-משתמש ← תיקון ← commit ← אימות). התבנית טרם אושרה סופית.
- **עיקרון שקודם לחוק כתוב** (CLAUDE.md, אחרי מקרה שני): פרוזת-מחולל לעולם אינה נושאת שם-תחנה או ממצא-תיק — אלה `region.json` בלבד; פסקי-דין בפרוזה נגזרים מספי-המנוע. נלווים: שיוך-נתיב גובר על קרבה-עירומה בסיווג-משפחות; **שומר-התיישנות** משווה `report_narrative.updated` מול `claims.updated` (עורך נרטיב — עדכן חותמת, אחרת באנר-אזהרה).
- **מבחן הצטרפות-עומס בצמתים** (#13, סגור): 4 גלאי-מקטע במנוע (`junction_scan`) + חוק ב-CLAUDE.md + ספים ב-config — רצים אוטומטית בכל תיק. מזהים מקור-שני **גם כשהרכבו דומה למוקד**, דרך יחסים ולא דמיון-כולל. קישון 5 אינדיקציות, חגית 3, קיסריה 0.
- **⚠ D2 של חגית דורשת עיון מחדש**: המבחן החדש חשף שהזרקת ראש-סדרת-הדעיכה לא כיבדה החרגת מוצאים-רגעיים. אחרי התיקון הדעיכה העילית **r=−0.7 (n=5)** במקום r≈0 שעליו נשענה השהיית D2.
- **ערוץ גיליון-תשובות ב-Artifact** (מתועד ב-CLAUDE.md): `downloads` מבטל את ההנחה "HTML סטטי לא קולט תשובות" — אך זה ערוץ-איסוף בלבד; ההכרעה נכנסת לתוקף רק ב-commit עם provenance.
- **מבחן-קישון עיוור בוצע ועבר**: שלב א' committed עם sources ריק (`fb77028`) לפני הכרזת מועמד (`ba4d14f`) — היסטוריית-git היא הוכחת-העיוורון. התקרה החזיקה ("מועמד משני" למרות 93% כימיה, r=-0.71); חריגי יקנעם/סולתם/יגור-בית צפו כ-KI-D2. תיק שלישי חי: `regions/kishon/`.
- **Footgun DEM**: אחרי הוספת מקור ל-sources.geojson חובה להריץ מחדש את `prepare_region_dem.py` — אחרת אין רשומת-מסלול למועמד ואיור-הדעיכה נעלם בשקט. אריחי Copernicus N32_E034/E035 מכסים את שלושת התיקים; ההורדה מ-AWS עובדת מהסביבה.
- **ערוץ Artifacts הוקם** (3 קישורים חיים, אצל המשתמש): חגית c1f624d7, קיסריה dfdfe31a, קישון bbbbb473 — לעדכון: פרסום-מחדש של אותו נתיב-קובץ. **Footgun**: קבצים שנשלחו בצ'אט (SendUserFile) נשארים כעותקים ישנים ומתחזים לעדכניים — טיוטות חיות דרך Artifact בלבד.
- **ממתין להכרעת המשתמש**: **D2 (עיון מחדש אחרי תיקון-החישוב)** · C1 (חגית-ליבה) · KD1 (שרשרת אור-עקיבא) · KI-A1/A2/A3 + KI-C1 + KI-D3 (קישון: קואורדינטה חשודה, כיוון-תהום 315° שהוא הנחת-מודל, סמנטיקת-חתך, דירוג, אינדיקציות-הצמתים) · תבנית #12 · פסקאות-המנגנון (טיוטות-מודל ב-region.json של שלושת התיקים).

---

## סשן 2026-07-27 (ב') — דוח-חקירה בפרוזה, פיצול-אמת, מטריצה קריאה

- **דוח-התיק הוא עכשיו דוח חקירה סביבתית-הידרולוגית בפרוזה** (`generate_case_report.py`) עם 4 איורים מוטמעים (Plotly inline מהחבילה המקומית — ללא CDN): מפה ITM, מטריצת-דמיון ממוספרת+מקרא, דעיכה, fingerprints. הנרטיב (רקע/עילה) נתונים ב-region.json (`report_narrative`).
- **פיצול-תיקים תוקן בשורש**: `_prepare` חותך לפי bbox — כל תוצר מציג אך ורק את תחנות-התיק (חגית 35, קיסריה 27). היה באג: הכל הראה 62.
- **המשימה המרכזית שנותרה (In flight)**: המשתמש ביקש **פרוזה מתארת ומסיקה עשירה יותר** — הדוח עדיין "טכני, נאמן-מתודולוגית, עני במלל". הפונקציות `_findings_prose`/`_discussion_prose` מחוללות טקסט מכני מרשימות-הראיות; צריך כתיבה נרטיבית-פרשנית אמיתית (סיפור הזיהום, תיאור גיאו-הידרולוגי, מסקנות עשירות).
- **חסם toolkit**: שיבוט `sessions-archive` נכשל סופית — הרשאת-GitHub מוצמדת בעת יצירת-הסשן, לא דינמית; אין כלי `add_repo` בערכה. **סשן חדש עם הריפו מצורף יפתור.** קונבנציות ה-toolkit יושמו מ-CLAUDE.md (bdi, print-rules, screenshot-QA).
- **QA חזותי הוא חובה** לכל שינוי בדוחות (playwright + `/opt/pw-browsers/chromium`, plotly זמין להטמעה מקומית; CDN חסום בסביבה שלי — לכן inline).

---

## סשן 2026-07-27 — סבב-הקיבוע: שני תיקים, מדרגות, מוצאים, דוח-לדוגמה

- **מבנה שני-תיקים**: `regions/hagit` (מערכת דליה, y>716k) + `regions/kesariya` (חוות חח"י, שקע-שאיבה). חולקים קובץ-מדידות; חיתוך לפי bbox. קואורדינטות רשמיות לשני האתרים.
- **ממתין להכרעת משתמש בפתיחת הסשן הבא**: (א) **C1 — חגית הועלה ל"מועמד ליבה"** (הראיות הנספרות אינן נשענות על הנחת-התהום) — הפיך; (ב) **תבנית דוח-התיק** — שני דוחות-לדוגמה נשלחו לקריאה, התבנית לא קובעה; (ג) מסגרת אטלס-מול-דשבורד — עדיין בבחינה.
- **D2 (יושרה)**: דעיכת-Σ העילית הושהתה (r≈0) — ערבוב שני משטרי-עומס סביב הסבת בריכה-1500 לביוב; ההזדקנות (r=-0.79) נותרה תומכת. יוכרע ב-ACT-3/ACT-4.
- **actions.json הוא תור-העבודה המבצעי** (5 פריטים בחגית, 3 בקיסריה) — לפתוח שם, לא לנחש.
- **Footgun תפר-הדומיין**: פרופילי-המקור נטענים מ-`domains/pfas/source_profiles.json`; הליטרלים ב-`src/source_profiles.py` הם fallback בלבד — עריכה שם לא תשפיע כשה-JSON קיים.
- מפת-המפלסים: קריאת-הצילום מקודדת כ-flow_zone (screenshot-quality) בקיסריה; קובץ נתונים יגיע בהמשך ויחליף.

---

## סשן 2026-07-26 — S1.5 + S2: עוצמה, דעיכה, DEM

- **אישורי משתמש מכריעים (provenance ב-region)**: (א) Σ=1,121 µg/L ב"חגית - בריכה 1500" **אמיתי — אזור-מקור ידוע**; מיקום המקור עוגן לתחנה זו (הניחוש הקודם סטה ~4.5 ק"מ). (ב) קובץ חגית = **מדידה אחרונה פר מקור** — חתך לא-בו-זמני, steady-state מוצהר. (ג) עומק-זמן נדחה.
- **מתודולוגיה חדשה עוגנה ב-CLAUDE.md** (סעיף "עוצמה, רעש וזמן"): סף-אות ראייתי, שקלול log-עוצמה, דעיכה כראיה, כלל צמוד-אתר, סמנטיקת חתך, עוגן-מקור.
- **S2 בוצע**: Copernicus GLO-30 הורד (רשת פתוחה דרך ה-proxy!), מנוע D8 טהור-numpy ב-`src/dem_engine.py`, נגזרות קלות ב-`regions/hagit/derived/` (732KB), `DemFlowModel` נטען אוטומטית לעילי. **rasterio נדרש רק ל-tools/, לא לאפליקציה.**
- **תוצאת חגית אחרי S2**: דעיכה על מרחק-מסלול r=-0.4 + הזדקנות פרופיל r=-0.8 → ראיות בעד; אבל מורד עילי אמיתי = 3 תחנות בלבד (מול 27 בהנחה); "דליה - בת שלמה" ובריכות הדגים **אינן** על מסלול הנגר של האתר → ראיית-נגד לגיטימית שדורשת הסבר (מקור נוסף? נתיב תהום?). תקרת הדירוג נשארת עד מפלסים (S3).
- **חוסם S3**: נתוני מפלסי תהום מהמשתמש.

---

## סשן 2026-07-22/23 — ניתוח אזורי S1 (פרופילי-מקור + ייחוס)

- **S1 נשלח** (`d7ac8da`): פרופילי-מקור, flow_model עם תיוג איכות, attribution עם תקרת-הנחה, ישות `regions/hagit/`, לשונית "🧭 ייחוס מקור". העקרונות עוגנו ב-CLAUDE.md (שלושה צירי ראיה, תקרת הנחה, ממשק-ספקים). פתוחים ב-PROCESS: #4 DEM עילי, #5 מחברי GIS, #6 תיקוני אפליקציה (מושהה).
- **העדפת משתמש מחייבת (footgun)**: הצעות תכנון — **מילולית בצ'אט בלבד**; המשתמש דחה במפורש כתיבת מסמך ARCHITECTURE/תוכנית לריפו ("תציע מילולית, לא לבנות תכנית שלימה"). ההחלטות מתועדות דרך PROCESS/CLAUDE בלבד לאחר אישור.
- **קואורדינטות תחנת הכוח חגית משוערות** (itm 204485,720260 — הוזנו ידנית) — ממתין לאימות המשתמש מול GIS רשמי; מסומן `approximate` ב-sources.geojson.
- **פערים ידועים**: מחוללי הדוחות (generate_report*.py) עדיין בלי פרק ייחוס; ה-URL של האפליקציה הפרוסה לא רשום בריפו (נמצא אצל המשתמש ב-share.streamlit.io); הפלט הטקסטואלי המלא של ריצת חגית נמסר בצ'אט בלבד, לא committed.
- **חוסמים ל-S3 (תהום)**: נתוני מפלסים מהמשתמש. ל-S2 (DEM): נדרש לבדוק גישת רשת להורדה, אחרת המשתמש יעלה.

---

## סשן 2026-07-22 — איחוד פרויקט + Claude Design

### החלטות מחייבות

1. **`Industrial-Areas-Report/` נמחק מהריפו** (החלטת משתמש, סשן זה).
   עותק מיושן היה; מקור האמת בריפו `haimk-max/industrial-areas-report`. README/NEXT_STEPS/CLAUDE/SUMMARY נוקו מכל הפניה אליו.

2. **יעד פריסה: Streamlit Cloud** — הריפו **הוכן לפריסה** (סשן זה). נותר רק ה-OAuth של המשתמש ב-share.streamlit.io (Main file path: `geo-forensics/app.py`, branch `main`). ראה סעיף "פריסה" ב-README.
   - **Footgun שנפתר:** Streamlit Cloud מריץ מ**שורש** הריפו, לא מ-`geo-forensics/`. לכן: (א) נוצר `requirements.txt` בשורש שמפנה ל-`geo-forensics/requirements.txt` (בלי שכפול); (ב) נוצר `.streamlit/config.toml` בשורש (מראה של זה שב-geo-forensics — Streamlit לא קורא config מתת-תיקייה כש-CWD=שורש). **שני קבצי ה-config חייבים להישאר מסונכרנים.**
   - `pyproj` שוחרר ב-requirements (היה מוער) — עכשיו backend הקואורדינטות המדויק; math fallback עדיין קיים אם חסר.

3. **הוכרע (סשן זה): הדשבורד הקנוני = עיצוב Clinical.**
   - `app.py` = הגרסה הקנונית (לשעבר `app_v2.py`) — עיצוב Clinical, לשוניות `st.tabs()`, KPI strip, insight cards, `html.escape` עקבי.
   - `app_legacy.py` = הגרסה המקורית (גלילה אנכית רציפה) — נשמרה להשוואה, לא לפיתוח.
   - הנימוק: עקביות עם `generate_report_v2` (אותה שפה ויזואלית), אבטחת HTML טובה יותר, וההשקעה ב-Claude Design.
   - במעבר הוחזרו מ-app_legacy 3 דברים שהיו עדיפים בו: **מתודולוגיה מלאה** (נוסף "חוזקות" לכל 5 ההסברים), **PCA/MDS במלוא הרוחב** (במקום זה-לצד-זה שהצטופף עם 32 תחנות), ותוקן ה-**MDS FutureWarning**.
   - `.streamlit/config.toml` עודכן לפלטת Clinical (accent `#2a9d8f`, bg `#f5f3ee`).

### מה נבנה בסשן זה

- **app.py** (קנוני): דשבורד Streamlit עם עיצוב Clinical מ-Claude Design (לשעבר app_v2.py).
- **app_legacy.py**: הגרסה המקורית, שמורה להשוואה.
- **generate_report_v2.py** (844 שורות): מחולל HTML v2 עם Compare Drawer (השוואת זוג תחנות).
- **report_hagit_v2.html / report_kishon_v2.html**: דוחות דוגמה שנוצרו ומוטמעים בריפו.

### קשיחות data-loader (נתוני רשות המים)

מקובץ אמיתי ("אזור חגית מורחב") התגלו שני דפוסים בייצוא רשות המים — שניהם טופלו ב-`src/data_loader.py`:
- **פרמטרים מצרפיים**: שורת `TPFAS` = סכום כל הקונגנרים. סוננה ב-`clean_data` דרך `AGGREGATE_PARAMETERS` כך שלא נכנסת ל-df (אחרת מנפחת Σ ומשתלטת על ה-fingerprint). הרחבת הרשימה = הוספת סמלים שם.
- **תאריכים כמספרים סידוריים של Excel** (למשל 45840): `pd.to_datetime` פירש אותם כ-1970. `_parse_dates` מזהה טווח סידורי (25569–73415) וממיר מ-origin 1899-12-30; מחרוזות/datetime עוברים כרגיל. בדיקות רגרסיה נוספו לשניהם.
- נותר פתוח (מינורי, לא טופל): 11 שורות עם `comparison_symbol="<"` וערך 0.001 נספרות כזיהוי במקום כאפס — השפעה ~0.011, זניח.

### שינוי שם ריפו → `PFAS-Forensics`

- **בוצע**: הריפו שונה מ-`my-first-project` ל-**`PFAS-Forensics`** (GitHub ממיר רווח למקף; אין "שם תצוגה" נפרד). הנתיב המלא כעת: `haimk-max/PFAS-Forensics`.
- ההתייחסויות הפנימיות עודכנו (README ×2 כולל הוראת הפריסה, CLAUDE ×1). git בסשן ממשיך לעבוד דרך ה-redirect של GitHub.
- **הפריסה בוצעה** (המשתמש צפה באפליקציה החיה): `haimk-max/PFAS-Forensics`, branch `main`, main file `geo-forensics/app.py`. תקלת-דרך שנפתרה: שתי אפליקציות OAuth של Streamlit ב-GitHub — הישנה ("Streamlit") מול "Streamlit Community Cloud" (שמעולם לא קיבלה הרשאה); נדרש grant לריפו פרטיים דרך האחרונה.
- **הצעד הבא של הסשן הבא**: המשתמש ראה את האפליקציה החיה ואמר "יש הרבה מה לתקן" — **רשימת התיקונים טרם נאספה**. לפתוח את הסשן באיסוף הרשימה מהמשתמש (רצוי עם צילומי מסך), לרשום ב-PROCESS.md, ורק אז לתקן. ה-UI מעולם לא עבר QA ויזואלי בדפדפן מצדנו — רק קומפילציה ובדיקות.
- ⚠ ה-redirect של GitHub יישבר אם ייווצר ריפו חדש בשם `my-first-project`.

### לקחים טכניים

- `st.html()` — DOMPurify מסיר `<link>` tags → פונטים חייבים דרך `@import` בתוך `<style>` ב-`st.markdown`.
- Plotly heatmap RTL: `reversescale=True` + `autosize=False` + `height` מפורש.
- **MDS FutureWarning תוקן**: `MDS(..., init="random")` — ב-sklearn 1.10 ברירת המחדל תשתנה ל-`classical_mds`; `init="random"` משמר את ההתנהגות הנוכחית.
- `st.tabs()` בסטרימליט אינו lazy — כל תוכן הלשוניות מרונדר בכל rerun (show/hide ב-CSS). אין רווח ביצועים; היתרון הוא ניווט בלבד. תלות סמויה: `pca_data` מחושב בלשונית PCA (05) ונצרך בלשונית ממצאים (06) — עובד כי 05 רץ לפני 06, אך שביר אם סדר הלשוניות ישתנה.

---

## סשן 2026-07-20 — אונבורדינג לכללים המעודכנים

- הפרויקט חובר לתשתית הממשל חוצת-הפרויקטים: CLAUDE.md נכתב מחדש (זהות אחת — GeoForensics PFAS; עקרונות מ-`CLAUDE.base@9801386`; ממשל קלט חיצוני; אובייקטיביות; תזכורת toolkit), הותקנו סקיל `/handover` + hook תחילת-סשן, נוצרו HANDOVER.md + PROCESS.md.
- **`Industrial-Areas-Report/` = עותק מיושן** (הכרעת המשתמש) — לא לפתח בו; מקור האמת בריפו `industrial-areas-report`. סעיפי הארכיטקטורה שלו (plugin-first, protocols) הוסרו מ-CLAUDE.md — זמינים בהיסטוריית git אם יידרשו.
- דוחות ה-HTML הקיימים (חגית/קישון) קדמו לתקני RTL/print של ה-toolkit — יישור ייעשה רק כשעובדים עליהם בפועל (לא רטרואקטיבית).

**הצעד הבא הסביר**: לפי `NEXT_STEPS.md` — בדיקה ויזואלית של הדשבורד, ייצוא HTML מהדשבורד, סדרות-זמן, סינון תאריכים. לא אושר עדיין כדרישה — לפתוח ב-PROCESS.md לפני עבודה.
