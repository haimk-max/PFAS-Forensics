# CLAUDE.md — GeoForensics PFAS Dashboard

## Project Purpose

פלטפורמה לניתוח גיאו-פורנזי של מזהמי PFAS במים, קרקע ושפכים — עבור השירות ההידרולוגי, רשות המים.
הפרויקט מייצר דוחות HTML אינטראקטיביים ודשבורד Streamlit מקבצי Excel, הכוללים ניתוחים סטטיסטיים, מפות וגרפים.
הליבה: `geo-forensics/`.

---

## עקרונות עבודה אדם–מודל

> הועתק מ-`sessions-archive/templates/CLAUDE.base.md@9801386` (הבית הקנוני; אין סנכרון אוטומטי — עדכון מהותי שם מחייב סבב-הפצה יזום).

### א. ממשל מסמכים ותיעוד
1. **עובדה אחת — בית אחד**: לכל עובדה (סטטוס, גרסה, החלטה, לקח) מקום קנוני יחיד; כל אזכור אחר הוא הפניה. שכפול עובדות — לא מספר הקבצים — הוא מקור הדריפט.
2. **מבחן קיום למסמך חדש**: מסמך מצדיק את קיומו רק אם הוא עונה על שאלה שאף מסמך קיים לא עונה עליה; אחרת — מיזוג.
3. **מקור אחד, פורמטים נגזרים**: תוצר בכמה פורמטים נכתב פעם אחת ומרונדר — אלא אם הפורמטים הם מסמכים מעוצבים בעבודת-יד עם תוכן עיצובי משלהם, שאז ריצת רינדור לא תבטל את העבודה הידנית ועדיף שומר-עקביות read-only.
4. **תיעוד גר ליד מושאו**: ממשל פר-פרויקט בריפו של הפרויקט; כל מה שחוצה-פרויקטים — בבית משותף אחד.
5. **טבלת תפקידי מסמכים**: קובץ הממשל מכיל טבלה קצרה "איזו שאלה כל מסמך עונה" — הפילטר למסמך הבא.

### ב. זיכרון ולמידה מתמשכת
6. **זיכרון בין-סשן committed**: הנחיות יציבות (CLAUDE.md) + מסירת-סשן נרטיבית (HANDOVER) + מעקב דרישות עם hash ושיטת אימות לכל סגירה (PROCESS). תוכניות בצ'אט נעלמות; קבצים נשארים.
7. **צינור קידום לקחים**: לקח מקודם לחוק כתוב רק אחרי מקרה שני שמאמת אותו.
8. **מטא-ביקורת תקופתית**: אחת לרבעון/אבן-דרך — בדיקת דריפט מסמכים, כלים לא-מסונכרנים, לקחים שהבשילו, ותוצרים בבית הלא-נכון.
9. **תעודת מעבר**: כל רעיון/תוצר שנודד בין כלים או פרויקטים מגיע עם בלוק FACT / ASSUMPTION / DECISION / OPEN — מה מחייב ומה רק השראה.
10. **רמת ודאות מוצהרת**: כל מסמך מסכם נפתח בהצהרת מקורות ומגבלות; טענות מהותיות מסומנות סטטוס אינליין.

### ג. עבודה עם מודלים
11. **תגובה מילולית לפני תכנון, תכנון לפני ביצוע**: ביקורת/משוב מקבלים קודם ניתוח ביקורתי מילולי; המודל מוזמן לחלוק ("לא מחויב למסקנות אך מחויב לשקול אותן").
12. **תיוג מוצא לכל קלט מודבק**: סקירה/משוב/טקסט נושא שורת מקור — אדם / מודל (איזה) / משולב.
13. **הקצאת מודלים לפי צפיפות-ערך**: מודל חזק לשיפוט; מודל זול לביצוע מובנה מבריף סגור.
14. **אימות מכני לעבודה מואצלת**: כל האצלה מלווה באימות שאינו תלוי בדיווח המבצע — SHA מול remote, צילום מסך, בדיקת מבנה. "בוצע בהצלחה" איננו ראיה.
15. **בדיקת השמטה ולא רק אימות**: בביקורת תוצרי מודל לחפש גם את מה שחסר, לא רק לאמת את הכתוב.
16. **שלב bootstrap מוצהר**: פרויקט שמומחה בונה לבד בסיוע AI מוגדר מראש כשלב עם נקודת-סיום שבה עובר לצוות/תשתית ארגונית.

### ד. חובת אובייקטיביות במחקר ובהערכת רעיונות
17. **השערה — לא הנחת-יסוד**: כשמתבקש מחקר או הערכה של רעיון (במיוחד רעיון/אבחנה של המבקש עצמו) — ההשערה נכנסת למחקר כהשערה לבדיקה מול הסברים מתחרים. אסור להטמיע אותה כהנחת-יסוד בבריפים לסוכני מחקר; בריף מוטה = מחקר מוטה, גם אם הממצאים נכונים בפרטיהם.
18. **חיפוש ראיות-נגד מובנה**: כל מחקר כולל ציר ייעודי לראיות סותרות ולהסברים חלופיים; טענות מכריעות-החלטה עוברות סבב הפרכה אדברסרי (סוכנים שמנדטם להפריך) לפני הצגת מסקנות.
19. **היגיינת ראיות**: בדיקת אינטרס-מקור (ספק שמוכר את הפתרון = חשד); בדיקת "מה קרה אחר-כך" לכל סיפור-הצלחה מצוטט; הבחנה בין מדדי-פרוקסי (השתתפות, תפוקה) לתוצאות בפועל (מימוש, אימוץ).
20. **חשד מוגבר להתאמה נוחה**: ממצא שמתיישב עם עמדה קודמת של המבקש מקבל בדיקה מחמירה יותר, לא מקלה; הדוח מציין במפורש היכן מסקנותיו חופפות לעמדות המבקש ועל איזה בסיס עצמאי הן עומדות. (מבחן-העזר: "אילו המבקש האמין בהיפך — האם המחקר הזה היה מגלה זאת?")

הפרוטוקול המלא למחקר: `sessions-archive/toolkit/process/playbooks/objective-research.md`.

---

## אילוצי סביבה

- המשתמש עובד **אך ורק** דרך Claude Code on the web — **אין גישה לטרמינל**.
- `~/.claude/` לא שורד בין סשנים. כל זיכרון בין-סשן חייב להיות committed לריפו.
- **זיכרון בין-סשן**: `HANDOVER.md` בשורש — קרא בתחילת כל סשן; סקיל `/handover` בסוף כל סשן.
- **מעקב דרישות**: `PROCESS.md` — SSOT לדרישות פתוחות/סגורות (hash + שיטת אימות לכל סגירה).

## קונבנציות שפה

- **קבצי Markdown** (ממשל, תוכניות, דוחות): **עברית**. **קוד**: **אנגלית**. **Commit messages**: אנגלית.
- **HTML עברי**: RTL מלא, `<bdi>` סביב טוקנים לטיניים, חצים ← בזרימת טקסט עברי. תבנית קנונית: `sessions-archive/toolkit/process/templates/hebrew-doc.html`; הדפסה: `toolkit/process/playbooks/print-ready-html.md`; QA חזותי: `toolkit/process/playbooks/screenshot-qa.md`.

## תזכורת toolkit (עדכון הבית הקנוני חוצה-הפרויקטים)

שינית/הוספת **כלי** שעשוי לשמש פרויקט אחר? עדכן את הבית הקנוני ב-`sessions-archive/toolkit/` **באותו סבב** ורענן את הערת ה-provenance בעותק המקומי. **אין סנכרון אוטומטי** — עותק הוא fork שמסטה. תהליכי ← `toolkit/process/`; דומייני ← `toolkit/domain/`. הבדיקה התקופתית — במטא-ביקורת.

## קשרי פרויקטים

| פרויקט | יחס |
|---|---|
| `water-knowledge-system` | פרויקט-אח — מערכת ידע ותשאול; אין תלות קוד |
| `sessions-archive` | הבית הקנוני לכלים תהליכיים, תבניות ו-KB |

---

## GeoForensics PFAS — מבנה הפרויקט
```
PFAS-Forensics/
├── CLAUDE.md                 # קובץ זה
├── SUMMARY.md                # סיכום יכולות המערכת
├── HANDOVER.md               # זיכרון בין-סשן
├── PROCESS.md                # מעקב דרישות
├── PFAS_INVESTIGATION_METHODOLOGY.md  # המתודולוגיה (בעל-גרסה; הבית הקנוני)
├── geo-forensics/
│   ├── generate_report.py    # מחולל דוחות HTML סטטיים (v1)
│   ├── generate_report_v2.py # מחולל דוחות HTML — עיצוב Clinical + Compare Drawer
│   ├── generate_review_report.py # דוח ביקורת-מומחה (מפה+ראיות+לוח טענות+גיליון תשובות)
│   ├── app.py                # דשבורד Streamlit — קנוני (עיצוב Clinical)
│   ├── app_legacy.py         # דשבורד Streamlit — גרסה מקורית (גלילה רציפה)
│   ├── config.py             # הגדרות: מפה, קואורדינטות, UI
│   ├── requirements.txt      # תלויות Python
│   ├── src/
│   │   ├── analytics.py      # Cosine similarity, ממצאים
│   │   ├── data_model.py     # עיבוד נתונים, fingerprint, ריכוזים
│   │   ├── data_loader.py    # טעינת קבצי Excel (מסנן מצרפים, תאריכי serial)
│   │   ├── geo_utils.py      # המרת קואורדינטות ITM ↔ WGS84
│   │   ├── contaminant_groups.py  # הגדרות קבוצות מזהמים
│   │   ├── source_profiles.py     # פרופילי-מקור PFAS (טרי/בלוי) + התאמה
│   │   ├── flow_model.py          # ספקי כיוון זרימה (הנחה→DEM→מפלסים)
│   │   ├── attribution.py         # מיזוג ראיות: מועמדים + בעד/נגד/מה-יפריך
│   │   └── generate_sample_data.py
│   ├── generate_case_report.py # דוח-תיק סביב משפחות-הסעה (מפת Leaflet, CSM, כרטיסי-נתונים, נספחים)
│   ├── tools/prepare_region_dem.py # הכנת DEM חד-פעמית (offline, rasterio) → נגזרות קלות
│   ├── tools/prepare_region_hillshade.py # הצללת-תבליט לרקע המפה (חד-פעמי)
│   ├── tools/prepare_region_basemap.py   # תצ"א Sentinel-2 לרקע המפה (חד-פעמי, AWS Open Data)
│   ├── vendor/leaflet/       # Leaflet 1.9.4 מקומי (BSD-2 + PROVENANCE) — מוטמע בדוחות
│   ├── domains/<דומיין>/     # ידע דומייני: פרופילי-מקור, רמזי-מוצר (בר-ביקורת מומחה)
│   ├── regions/<שם>/         # ישות אזור: region.json + sources.geojson + claims.json + actions.json (provenance); derived/ (ערוצי DEM, hillshade, basemap)
│   ├── data/sample/          # קבצי נתונים לדוגמה
│   ├── report_*.html         # דוחות שנוצרו (חגית, קישון; v1+v2)
│   ├── tests/                # בדיקות pytest
│   ├── .streamlit/config.toml # ערכת נושא (theme)
│   └── REQUIREMENTS.md       # מפרט דרישות הדשבורד
```

## פקודות הרצה

### דשבורד אינטראקטיבי (Streamlit)
```bash
cd geo-forensics
pip install -r requirements.txt
streamlit run app.py
```

### יצירת דוח HTML סטטי
```bash
cd geo-forensics && python generate_report.py "data/sample/דוגמה - חגית PFAS.xlsx" -o report_hagit.html
```
**חשוב:** תמיד להריץ מתוך תיקיית `geo-forensics/` — הסקריפט משתמש בנתיבים יחסיים.

### בדיקות
```bash
cd geo-forensics && pytest
```

## ניתוחים סטטיסטיים בדוח
| ניתוח | תיאור | ספרייה |
|-------|--------|--------|
| PCA | הפחתת ממדים, זיהוי אשכולות | scikit-learn |
| MDS | Multidimensional Scaling על מרחק קוסינוס | scikit-learn |
| Cosine Similarity | מטריצת דמיון בין תחנות | scipy |
| Hierarchical Clustering | קיבוץ היררכי של תחנות | scipy |
| ΣPFAS Concentration | ריכוז כולל בציר לוגריתמי | pandas/numpy |

## טכנולוגיות
- **Python**: pandas, scikit-learn, scipy, numpy, openpyxl, pyproj
- **HTML Reports**: Plotly.js (גרפים), Bootstrap RTL (עיצוב), Leaflet.js (מפות)
- **Dashboard**: Streamlit, streamlit-folium
- **קואורדינטות**: pyproj — המרה ITM (EPSG:2039) ↔ WGS84 (EPSG:4326)

## כללי עבודה
- קבצי Excel בעברית — שמות עמודות ותחנות בעברית.
- ה-HTML שנוצר self-contained — כל הנתונים מוטמעים כ-JSON.
- הדוח תומך RTL (עברית); דוחות חדשים מיישרים לתקני ה-HTML שבקונבנציות השפה לעיל.
- קבצים גדולים מאוד (>1MB) — לוודא שהניתוח לא קורס על זיכרון.
- **ניסוח ייחוס זהיר** (קריטי משפטית) — ראו "מתודולוגיה — הבית הקנוני" למטה.
- **פרוזת-מחולל נקייה מממצאי-תיק** (קודם לחוק אחרי מקרה שני — מ-4 ומ-6, 2026-08-11): קוד המחוללים לעולם אינו נושא שם-תחנה, אירוע או ממצא אמפירי של תיק ספציפי — ממצאים פר-תיק חיים ב-`region.json` (`observed_he` וכד') ומודפסים רק בתיקם; פסקי-דין בפרוזה (תומך/נגד/לא-חד-משמעי) נגזרים מספי-המנוע, לא מנוסח קבוע. טקסט תיק-ספציפי בקוד מותר רק בבלוק מגודר-שם (`region=="hagit"`).
- **רגרסיית-טענות אחרי שינוי-מנוע** (חוק מ-2026-08-16): שינוי בחישוב/סינון/נגזרות מזיז בשקט מספרים שטענות ב-`claims.json` מצטטות — הבדיקות עוברות ושום דבר לא נכשל. לפני commit: הרצה לפני/אחרי, ‏diff מכני, הצלבה מול לוח-הטענות והכרעה פר-טענה. הפרוטוקול המלא: `sessions-archive/toolkit/process/playbooks/engine-change-vs-claims.md`.
- שינויים הרסניים (`reset --hard`, `force push`, מחיקת ענפים/תיקיות) — אישור מפורש בלבד.

## מתודולוגיה — הבית הקנוני

**כל המתודולוגיה חיה במסמך אחד: `PFAS_INVESTIGATION_METHODOLOGY.md` (גרסה 1.0).** שם מוגדרים: מבנה הקלט וכללי הסינון, טיפול בערכים מתחת לסף-כימות, בניית הפרופיל הכימי ומדידת הדמיון, ארבעת רכיבי התמיכה הכימית, ניתוח הבליה, ספי איכות-האות והאשכולות, מודל הזרימה והנתיבים, הנחות התהום ומדרגותיהן, סיווג קידוחים, לוגיקת דירוג-המקורות ואוצר-הדירוג, רמות הוודאות והמגבלות.

**אין לשכפל את הכללים לכאן ולא לדוחות** — קובץ זה מפנה, הדוחות נושאים חותמת-גרסה. שינוי מתודולוגי = עדכון המסמך + העלאת גרסה + רישום ב-`PROCESS.md` + בדיקת השפעה על התיקים הקיימים.

תזכורות תפעוליות בלבד (הפירוט — במסמך המתודולוגיה):
- **ניסוח ייחוס זהיר** (קריטי משפטית): "מועמד מרכזי" / "מועמד משני" / "מקור אפשרי" / "לא נתמך בשלב זה" — **לעולם לא "המקור"**. אוצר-מילים זה נקבע ב-v1.0; טבלת המיגרציה מהאוצר הישן ("מועמד ליבה" וכו') נמצאת במסמך המתודולוגיה §14.
- **התמיכה הכימית לעולם אינה מוצגת כ-"XX% התאמה"** — ארבעה רכיבים נפרדים (דמיון 0–1, התאמה לבליה, איכות אות, רמת תמיכה).
- **צירי הראיה הם "משלימים"**, לא "בלתי-תלויים".
- **"מונוטוני" רק כשהנתונים תומכים** בירידה רציפה; אחרת "מגמת ירידה כללית... אינה רציפה", וסטיות מוצגות כתוספת-זיהום אפשרית.
- **ייחוס AFFF ברמת משפחה/טכנולוגיה** — לא ברמת מוצר מסחרי.
- **פרמטרים ברי-כיול** מרוכזים ב-`geo-forensics/config.py` ומתועדים בנספח המתודולוגיה.
- **תיקים מרובים**: תיק = `regions/<שם>/` עם bbox משלו; לכל תיק claims + actions + דוחות.
- **תפר core/domain**: ידע דומייני = נתונים ב-`domains/<דומיין>/` (בר-ביקורת מומחה); המנועים ב-`src/` גנריים.

## לולאת ביקורת-מומחה (איטרציות אישרור/דחייה/תיקון)
- **לוח טענות ממוספר** (`regions/<שם>/claims.json`): רק פריטים שהכרעתם משנה מסקנה (מבחן השפעה). שלוש משפחות — A הנחות, D נגזרות, C מסקנות; לכל טענה: statement, status, impact ("אם יידחה — מה מתהפך"), ומיקום ITM. מקור-אמת יחיד שגם הצ'אט וגם דוח-הביקורת קוראים ממנו.
- **הכרעות בצ'אט, לא באפליקציה**: שורת `מזהה ✓/✗/תיקון` — נכתבת ל-git עם provenance. Streamlit Cloud ארעי (כתיבה לא שורדת); דוח-HTML סטטי לא קולט תשובות. הדוח פותר *ראייה והבנה* באזור זר, לא מחליף את ערוץ ההכרעה.
- **ערוץ-עזר: גיליון-תשובות אינטראקטיבי (Artifact)** (עודכן 2026-08-11): דף-Artifact עם יכולת `downloads` כן קולט סימוני ✓/✗/תיקון ומייצא קובץ-תשובות — ההנחה "HTML סטטי לא קולט תשובות" אינה חלה על Artifacts. זה ערוץ-איסוף בלבד: הקובץ המיוצא מועלה חזרה, נרשם ל-git עם provenance, וההכרעה נכנסת לתוקף רק ב-commit. הצ'אט נשאר ערוץ-ההכרעה הקנוני. **הפצת טיוטות חיות — בקישורי-Artifact** (מתעדכנים באותו URL); קובץ שנשלח בצ'אט הוא עותק קפוא שמתיישן.
- **הבחנה קריטית — הצהרה מול השערה**: הצהרה (`water_transfers`) מזיזה תחנות למורד; השערה (`transfer_hypotheses`) רק מסמנת "ראיית-נגד מותנית" בלי להזיז. מונע אישור-עצמי.
- **נתונים ≠ הכרעות**: קואורדינטות/מקורות/GIS — דרך העלאת קובץ ו-loader שמאמת; שיפוט — דרך צ'אט.
- **דוח-הביקורת** (`generate_review_report.py`): מרכיב את הקבצים הקיימים (בלי לוגיקה כפולה) לעמוד HTML — מפה, גרפי דעיכה, ראיות בעד/נגד/מה-יפריך, לוח הטענות, וגיליון-תשובות מוכן להעתקה.

## עקרון ממשק-ספקים (מודולריות)
כל שכבת הקשר (כיוון זרימה, מקורות פוטנציאליים, בהמשך מפלסים ושכבות GIS) נכנסת דרך ממשק צר עם תיוג איכות (`assumed` / `derived_dem` / `derived_heads` / קובץ / GIS ארגוני). היום — הנחות וקבצים ב-`regions/`; בעתיד — מחבר למערכת המידע הארגונית של הרשות, **בלי לגעת בלוגיקת הניתוח**. כל שכבה נושאת provenance: מקור, תאריך, איכות.

## כללי מימוש מפה (דוח-התיק, איור 1)

> הכללים הגנריים (לא-PFAS) הועלו לבית הקנוני החוצה-פרויקטים: `sessions-archive/toolkit/process/playbooks/leaflet-maps-rtl.md@4d4bdd7`. שינוי מהותי כאן מחייב עדכון שם באותו סבב — אין סנכרון אוטומטי.

- **Leaflet מוטמע, לא Plotly** (2026-08-12): המפה רצה על Leaflet 1.9.4 שנשמר בריפו (`geo-forensics/vendor/leaflet`, ‏BSD-2 + PROVENANCE) ומוזרק inline — דף-Artifact חסום מ-CDN. ‏CRS.Simple ביחידות ITM-ק"מ (lat=Y, lng=X): אין הטלה ואין שרת-אריחים. Plotly נשאר לשאר האיורים.
- **`preferCanvas: true` חובה**: רנדרר-ה-SVG של Leaflet לא נותן hit-testing אמין בתוך המסמך ה-RTL הזה (המצביע נופל ל-container) — עם canvas ה-tooltip/popup עובדים, וגם הביצועים בעשרות-מאות סמנים.
- **תוויות אינן חוסמות לחיצות**: כל עטיפת-DivIcon של טקסט (`.stnlblwrap/.stnlbl/.srclbl`) חייבת `pointer-events:none` — היא גדולה מתיבת-האייקון ובולעת לחיצות מהסמנים שמתחתיה.
- **רקעים נגזרים offline ומוטמעים**: `tools/prepare_region_basemap.py` (תצ"א Sentinel-2 L2A מ-AWS Open Data, ‏`sentinel-cogs`) ו-`tools/prepare_region_hillshade.py` (הצללת-תבליט מה-DEM) — שניהם רצים פעם אחת פר-תיק, נשמרים ב-`regions/<שם>/derived/` ומוטמעים כ-data-URI. ייחוס חובה: "מכיל נתוני Copernicus Sentinel-2 מעובדים (ESA)" + תאריכי-הסצנות; הרקע הוא **קריאוּת בלבד — לא שכבת-ראיה**, ותאריכו שונה מתאריכי הדיגום.
- **בחירת סצנת-לוויין לפי כיסוי-נתונים בפועל**, לא רק לפי אחוז-ענן: מסלולי Sentinel-2 מתחלפים ומשאירים כמחצית מהסצנות ריקות מעל פינת-אריח נתונה — הכלי בודק את חלון-ה-bbox ודורש ≥50% פיקסלים עם נתונים.
- **ניווט**: סרגל-תצוגה בכפתורים גדולים (כל האגן / מכלול פר-מועמד / מקטע-הצמתים / ±) מעל המפה — לא להסתמך על פקדים זעירים; תיבות-שכבות ל-רקע/ערוצים/מסלולים/מקורות/צמתים/תוויות; הדפסה מאפסת לתצוגת כל-האגן עם כל השכבות.

## כללי מימוש UI (Streamlit)
- **פונטים**: `@import url(...)` בתוך `<style>` ב-`st.markdown` (לא `st.html()` — DOMPurify מסיר link tags).
- **כרטיסים**: `st.container(border=True)` + `st.metric()` (לא HTML divs).
- **מפה CSS**: `folium.DivIcon` עם inline styles (לא Tooltip permanent).
- **Multiselect chips**: CSS ב-`st.markdown` מכוון ל-`[data-baseweb="tag"]` בתוך `section[data-testid="stSidebar"]`.
- **ערכת נושא**: `.streamlit/config.toml`.

## טבלת תפקידי מסמכים

| מסמך | השאלה שהוא עונה עליה |
|------|----------------------|
| **CLAUDE.md** | מהם הכללים הקבועים לעבודה בפרויקט? |
| **PFAS_INVESTIGATION_METHODOLOGY.md** | מהי המתודולוגיה המדעית, באיזו גרסה, ולפי אילו ספים וכללי-דירוג? |
| **PROCESS.md** | מה פתוח ומה נסגר (hash + אימות)? |
| **HANDOVER.md** | מה הסשן האחרון הותיר שהבא צריך לדעת? |
| **SUMMARY.md** | מה המערכת יודעת לעשות (יכולות)? |
| **geo-forensics/REQUIREMENTS.md** | מהו מפרט הדשבורד? |
